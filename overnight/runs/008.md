# Overnight Execution 008

## Status

- Campaign: `paia-overnight-20260914-v1`
- Execution: **008 / final admitted opportunity**
- Slice: **ON-02**
- Official UX round: **UX-R5**
- Mode: **VERIFY_ON02_CI_ONLY**
- Result: **WAITING_CI**
- Product start HEAD: `19fe4ed19bc686ff715c5604616ea7fad1eed2fa`
- Product end HEAD: `6517763883c95e9a8901e5095db2a7d846da0ef6`
- End tree: `6ded7b5ba60fec390048200b54bda8422d2aaba9`
- Lint: **NOT_CONFIGURED**

## Entry / ownership checks

- Product `ux-r2` matched the State exactly before work.
- Frozen CI base remained `ad386c07cff59b9b3472a5aa03626fe89514f8d1`.
- Draft PR #28 remained open, draft, unmerged, base `overnight/ci-base-20260914`, head `ux-r2`.
- `main` was read only and was not modified or merged.
- Authority documents were read from the verified product head. UX-R5 remains the official current round; no UX-R6 work was started.

## Implementation summary

The published ON-02 candidate closes the missing explicit AI-update comparison path without introducing a new content truth store:

1. Updating an existing AI-organized Topic keeps the current presentation readable and stores a separate bounded candidate instead of replacing the current draft.
2. Candidate validity is tied to the exact current presentation revision and a body-free Topic material-version snapshot. A human edit or material change marks the candidate stale and blocks save.
3. The UI stages `adopt` / `keep` decisions locally for every changed AI field. Section choices make no write. `保存这些选择` submits the complete decision set once; the domain applies it in one CAS-guarded transaction and one presentation revision.
4. Adopted and explicitly kept changed sections become human-protected. Incomplete/extra decisions are rejected.
5. Candidate evidence has its own deletion fence. Source purge removes an affected candidate while preserving a still-valid current presentation; existing stronger current-presentation invalidation remains.
6. Candidate state stays inside the existing `aiPresentation:<topic>` organization-state row. Backup validates candidate evidence and drops an unsafe candidate rather than adding another body store or schema family.
7. `OUTCOME_UNKNOWN` no longer exposes an automatic retry action; status reads remain local and another paid call requires a new explicit user action.
8. Added domain tests and a real-browser ON-02 journey for candidate isolation, atomic save, concurrent stale state, purge, Backup round-trip, no-delta/status zero requests and current-draft preservation.

## Published diff

Single-parent fast-forward from `19fe4...` to `651776...`, `force=false`.

11 files, 220 additions / 25 deletions:

- `extension/core/backup-service.js`
- `extension/core/organizer/ai-candidate.js`
- `extension/core/organizer/ai-presentation.js`
- `extension/core/organizer/metadata.js`
- `extension/tests/ux-r5-ai-candidates.test.mjs`
- `extension/tests/ux-r5-ai-states.test.mjs`
- `extension/tests/ux-r5-ai-update-chrome-e2e.test.mjs`
- `extension/ui/action-feedback.js`
- `extension/ui/ai-candidate.js`
- `extension/ui/thought-reader.css`
- `extension/ui/thoughts.js`

No manifest permission, provider, credential model, canonical Source/Input/Thought body store, schema version, automatic Provider trigger, timeout relaxation, force push, merge or main change is included.

## Validation

Before publication:

- Direct unit/domain: **NOT_RUN** — no checked-out repository/runtime is available in this automation environment; clone attempt could not resolve GitHub DNS.
- Direct browser: **NOT_RUN** for the same reason.
- Direct package/static/build: **NOT_RUN** for the same reason.
- Lint: **NOT_CONFIGURED** by the repository.
- Exact diff review: **PASS_PREPUBLICATION_REVIEW**.

After publication:

- PR #28 triggered **PAIA Certification #263**, run id `34871354407`, for exact product head `6517763883c95e9a8901e5095db2a7d846da0ef6`.
- At this checkpoint the run is queued. No CI PASS is claimed yet.

## Acceptance / remaining work

ON-02 is **not complete merely because the implementation is published**. The exact tree must pass every mandatory CI job, unsharded full-suite receipt, current real-browser suite, package/development/release guards and visual/behavior evidence review. If the published tree fails, execution 008 may repair only ON-02 while its existing time window remains; no later slice is allowed.

There are **0 campaign opportunities after execution 008**. UX-R6, final UX-R1→R6 regression, Apple and MCP work remain NOT STARTED. If Certification #263 is still pending at final handoff, the campaign ends with `WAITING_CI` rather than an invented completion claim.
