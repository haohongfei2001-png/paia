# Execution 002 — ON-01 implementation

Campaign: `paia-overnight-20260914-v1`  
Execution opportunity: **2**  
Mode: **IMPLEMENT_ON01 → WAITING_CI**  
Slice: **ON-01 / UX-R5 topic-local dual view and safe first generation**  
Status: **PUBLISHED; REQUIRED CERTIFICATION PENDING**

## Product HEADs

- Start: `c8bcecd4c8a49a069e66225c199fc3d49c7b8b1b`
- End: `825c1644da4effd104ad6d250aa2344b9b4a0ca8`
- End tree: `c79ce29c53c18ca45236d6b81c8f79b181282a8b`
- Publication was a `force=false` fast-forward after the candidate was pre-registered on the control branch.
- `main` was not modified or merged.

## Changed files

1. `.github/workflows/paia-certification.yml`
2. `extension/core/topic-ai-view-session.js`
3. `extension/tests/ux-r5-ai-organize-chrome-e2e.test.mjs`
4. `extension/tests/ux-r5-on01.test.mjs`
5. `extension/ui/ai-first-generation.js`
6. `extension/ui/thoughts-base.js`
7. `extension/ui/thoughts.js`

## Implementation summary

- Added bounded, tab-local per-Topic Original/Organized state with separate scroll/query/page positions; a newly opened Topic defaults to Original rather than inheriting legacy global `libraryView`.
- Topic view switching flushes active edits first, keeps existing transition/focus behavior, and does not write the legacy global view preference.
- An explicit AI search/history target may carry one Organized-view intent into its target Topic without changing defaults for other Topics.
- First generation keeps Original visible, shows current-Topic scope and provider (`DeepSeek`), and requires a second explicit confirmation before the existing provider request path is invoked.
- Cached Organized content remains a local read/switch path and does not require a provider call merely to view it.
- Navigation away from a Topic during an in-flight first-generation request does not intentionally reopen the Topic or start a duplicate paid request; completion can settle into the existing cache.
- Local-only and Source-purge paths are explicitly covered by new browser tests.
- ON-02 candidate comparison/adoption was deliberately excluded.
- Existing ThoughtWorkspace implementation was preserved byte-for-byte as `thoughts-base.js`; the new `thoughts.js` is a narrow R5 coordinator subclass. This is source decomposition only, not a durable data/body layer.

## Validation / evidence

- Candidate diff review before publication: **PASS** for the exact seven-file set; no ON-02 candidate module was included.
- Local executable environment: **UNAVAILABLE** — no authorized remote desktop device was available, so no local commands are claimed.
- Unit: **PENDING_CI**
- Browser: **PENDING_CI**
- Adapter contract: **PENDING_CI**
- Privacy/security: **PENDING_CI**
- Package check: **PENDING_CI**
- Development audit: **PENDING_CI**
- Full suite: **PENDING_CI**
- Release build: **PENDING_CI**
- lint: **NOT_CONFIGURED**
- CI: PAIA Certification **#258**, run id `34838202885`, observed **queued** immediately after publication.

## Unresolved risks

- No completion claim is made until Certification #258's required jobs, merge/check tree, full-suite receipt and R5 browser evidence are inspected.
- Source decomposition may expose assumptions in package/source-inspection tests; if a required gate rejects it, the next execution must repair the implementation rather than weaken the gate.
- The new browser journeys and wrapper syntax have not yet been independently executed outside CI.

## Next recommended action

The next execution must begin with Certification #258. If any implementation, test, build, diff or evidence gate fails, repair ON-01 only and recertify. Do not begin ON-02 until ON-01 is actually certified and recorded COMPLETE.
