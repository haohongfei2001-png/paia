# Execution 003 — ON-01 repair

Campaign: `paia-overnight-20260914-v1`  
Execution opportunity: **3**  
Mode: **REPAIR_ON01**  
Slice: **ON-01 / UX-R5 topic-local dual view and safe first generation**  
Status: **WAITING_CI**

## Product HEADs

- Start: `825c1644da4effd104ad6d250aa2344b9b4a0ca8`
- End: `ad889dc59ff78d1318515ac56d4d571125997cec`
- End tree: `48e9e2377c17391f8ba2f008ce39d64f60f142fb`
- Publication was a single-parent `force=false` fast-forward.
- `main` remained read-only and was not merged.

## Why this execution repaired instead of progressing

PAIA Certification #258 for execution 002's ON-01 commit failed the required package audit on:

`ui/thoughts-base.js:24: forbidden keyboard listener`

Inspection confirmed this is the existing scoped Library menu Escape handler, moved byte-for-byte from the previously reviewed `ui/thoughts.js` path by ON-01's source decomposition. The functional test groups inspected before the audit failure did not identify a separate behavior failure.

Repair-before-progress therefore applied. No ON-02 candidate/update work was started.

## Changed files

Execution 003 changes exactly one product file relative to its start HEAD:

- `extension/scripts/check_package.py`

The ON-01 runtime implementation from execution 002 is otherwise unchanged.

## Implementation summary

The conservative static package audit now recognizes the moved Library menu Escape handler only at `ui/thoughts-base.js`, with a stricter condition than a general path allowlist:

- exactly one matching Library menu keydown call must exist;
- the complete Escape-only handler text must match the reviewed behavior;
- only that one call is replaced before the generic forbidden-keyboard scan;
- every other keyboard listener remains forbidden.

The repair does not change extension runtime behavior, manifest permissions, network/provider behavior, trusted sender scope, Source/Input/Thought semantics or migration state. It does not add ON-02 behavior.

The candidate diff was reviewed before publication: one file, the narrow audit addition plus a newline-at-EOF formatting artifact. No unrelated file entered the repair commit.

## Validation / CI

### Failed predecessor — PAIA Certification #258

#258 remains failure evidence only.

- Inspected Unit 1/4 functional tests: **276/276 PASS**, 0 fail before static package audit.
- ON-01 unit coverage in that shard passed.
- Adapter contract: **95/95 PASS**, 0 fail before static package audit.
- Development privacy/permission/network audit in inspected jobs: **PASS**.
- macOS Secure Store Certification: **PASS**.
- Required package audit: **FAIL** on the moved listener path, so the round was not accepted.
- #258 was still completing/cancelling obsolete work after the new product head was published; none of its partial successes are completion evidence for the repaired head.

### Repaired candidate — PAIA Certification #259

- Run id: `34840192608`
- Run number: **259**
- Product head: `ad889dc59ff78d1318515ac56d4d571125997cec`
- Product tree: `48e9e2377c17391f8ba2f008ce39d64f60f142fb`
- State at seal: **pending**; jobs had not yet materialized in the run API.
- Required unit/browser/contracts/package/full-suite/release/macOS/final certification evidence: **NOT YET VERIFIED**.

Local runtime/tests: **NOT_RUN** — no authorized local/desktop execution environment was available in this automation context.  
Lint: **NOT_CONFIGURED** — no separate lint command is part of the repository's required UX command set.

## Acceptance status

ON-01 remains **PENDING_CI**, not COMPLETE. The static-audit defect has a published bounded repair, but completion still requires every mandatory job in #259, current browser evidence, full-suite receipt, release build and final Certification gate to pass on the repaired product head.

## Unresolved risks / blockers

- #259 may still expose a runtime, browser, package, migration or acceptance failure. Such a failure must be repaired before progress.
- The repaired static audit is intentionally exact; any future change or duplication of that moved Escape handler will fail closed and require review.
- Official `UX_IMPLEMENTATION_STATUS.md` correctly remains UX-R5 READY / NOT STARTED until the full R5 round is completed; ON-01 is only a bounded internal overnight slice.

## Next recommended action

Next execution must first verify `ux-r2`, control ownership, the frozen CI base and Draft PR #28, then inspect **all** PAIA Certification #259 jobs, logs/receipts/artifacts and the actual product diff. If any required gate fails, repair ON-01 again. Only an all-green verified #259 may close ON-01 and permit later ON-02 work.
