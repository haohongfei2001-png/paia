# Execution 003 — ON-01 repair

Campaign: `paia-overnight-20260914-v1`  
Execution opportunity: **3**  
Mode: **REPAIR_ON01**  
Slice: **ON-01 / UX-R5 topic-local dual view and safe first generation**  
Status: **PREPARED FOR PUBLICATION**

## Product HEADs

- Start: `825c1644da4effd104ad6d250aa2344b9b4a0ca8`
- Prepared candidate: `ad889dc59ff78d1318515ac56d4d571125997cec`
- Candidate tree: `48e9e2377c17391f8ba2f008ce39d64f60f142fb`
- End: pending publication
- `main` remains read-only and was not merged.

## Trigger for repair

PAIA Certification #258 for the exact ON-01 product candidate reached required-job failures. The representative adapter and Unit 1/4 logs show their functional tests passing, followed by the same package guard rejection:

`ui/thoughts-base.js:24: forbidden keyboard listener`

The listener is the previously reviewed scoped Library menu Escape handler that had lived in `ui/thoughts.js`; ON-01's source decomposition moved the unchanged handler to `ui/thoughts-base.js`, invalidating the static audit's exact reviewed-path exception.

## Evidence from failed certification #258

- PR merge checkout head matches expected product commit `825c1644da4effd104ad6d250aa2344b9b4a0ca8`.
- macOS Secure Store Certification: **PASS**.
- Unit 1/4 functional execution: **276 PASS / 0 fail** before package audit; ON-01 unit coverage passed in that shard.
- Adapter contract: **95 PASS / 0 fail** before package audit.
- Package audit: **FAIL** on the single moved Library menu listener path.
- Development privacy/permission/network audit in the inspected jobs: **PASS**.
- #258 is failure evidence, not completion evidence. No required group is promoted to PASS merely because its tests completed before the audit failure.

## Prepared repair

The candidate changes only `extension/scripts/check_package.py`. It does **not** modify runtime UI behavior, manifest permissions, provider/network behavior or ON-02 logic.

The initial claim proposed moving the listener back under `ui/thoughts.js`. Diff review showed the safer and smaller repair is to keep the ON-01 source decomposition and update the conservative static audit for the exact moved call site. The new rule:

- applies only to `ui/thoughts-base.js`;
- requires exactly one matching `menu.addEventListener('keydown', ...)` call;
- requires the full reviewed Escape-only handler text to remain unchanged;
- replaces only that one exact audited prefix before the existing generic forbidden-keyboard scan;
- leaves every other keyboard listener forbidden.

This is not a blanket allowlist expansion and does not lower the acceptance criteria. If the reviewed handler is duplicated or changed, package audit fails closed.

## Candidate diff review

`825c1644...` → `ad889dc5...` is one file only: `extension/scripts/check_package.py`, with the six-line narrow audit plus an EOF-newline formatting artifact. No runtime source changed in the candidate.

## Validation status before publication

- Local runtime/tests: **NOT_RUN** — no authorized local/desktop execution environment is available in this automation context.
- Lint: **NOT_CONFIGURED** — repository has no separate lint command in the required UX command set.
- Static/package validation for the prepared candidate: **NOT_RUN locally**; must be proved by the new PAIA Certification run after publication.
- Required browser/full-suite/release validation: **NOT_RUN on candidate**; must be proved by the new CI run.

## Publication rule

Before moving `ux-r2`, re-read the product ref and control ownership. Publish only if product remains exactly `825c1644da4effd104ad6d250aa2344b9b4a0ca8`, the control branch remains owned by execution 003, the frozen CI base and Draft PR #28 are unchanged, and the candidate remains a single-parent fast-forward. Use `force=false`.

After publication, execution 003 must seal as `WAITING_CI`; ON-02 must not begin in this execution. The next execution must inspect the complete new CI jobs/receipts/diff and either repair ON-01 again or mark it complete.
