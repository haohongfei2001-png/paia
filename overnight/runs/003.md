# Execution 003 — ON-01 repair

Campaign: `paia-overnight-20260914-v1`  
Execution opportunity: **3**  
Mode: **REPAIR_ON01**  
Slice: **ON-01 / UX-R5 topic-local dual view and safe first generation**  
Status: **IN PROGRESS**

## Product HEADs

- Start: `825c1644da4effd104ad6d250aa2344b9b4a0ca8`
- End: pending
- `main` remains read-only and must not be merged.

## Trigger for repair

PAIA Certification #258 for the exact ON-01 product candidate reached multiple required-job failures. The representative adapter and Unit 1/4 logs show their functional tests passing, followed by the same package guard rejection:

`ui/thoughts-base.js:24: forbidden keyboard listener`

The listener is the previously reviewed scoped Library menu Escape handler that had lived in `ui/thoughts.js`; ON-01's source decomposition moved it to a new path, invalidating the static audit's exact reviewed-path exception. This execution will repair the implementation/source decomposition rather than weaken or broaden the package guard.

## Validation status at claim

- `ux-r2` and control refs matched state.
- Frozen CI base matched `ad386c07cff59b9b3472a5aa03626fe89514f8d1`.
- Draft PR #28 remained `ux-r2` → frozen CI base and unmerged.
- CI #258 checked out merge `531520f34b104f1171fd1f22af0bff7e92bd4133`, whose head is the expected product commit.
- macOS Secure Store job already PASS.
- Unit 1/4 functional tests: 276 PASS / 0 fail before package audit; ON-01 unit tests passed.
- Adapter contract: 95 PASS / 0 fail before package audit.
- Package audit: FAIL on the single moved listener path.
- Other required jobs were still running or failing through the same audit when this repair was claimed.

## Planned repair

Keep the reviewed keyboard listener on `ui/thoughts.js` and move only the R5 coordinator/subclass to a separate module used by `archive.js`, removing the temporary `thoughts-base.js`. Do not modify `check_package.py`, permissions, provider/network behavior or ON-02 candidate/update behavior.

Final changed files, tests, CI, risks and end HEAD will be recorded before sealing this execution.
