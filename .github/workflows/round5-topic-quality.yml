name: Round 5 Topic Quality

on:
  push:
    branches:
      - round5/topic-quality-stability
    paths-ignore:
      - 'extension/V0111_ROUND5_TOPIC_QUALITY.md'

permissions:
  contents: read

concurrency:
  group: round5-topic-quality-${{ github.ref }}
  cancel-in-progress: true

jobs:
  topic-quality:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    defaults:
      run:
        working-directory: extension
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Focused Round 5 regressions
        run: node --test --test-concurrency=1 tests/topic-quality-round5.test.mjs tests/topic-quality-v080.test.mjs tests/deepseek-provider-v072a.test.mjs
      - name: Deterministic Topic stability benchmark
        run: node scripts/topic-quality-round5-benchmark.mjs
      - name: Broad portable unit suite
        run: |
          node --input-type=module <<'NODE'
          import {readdirSync} from 'node:fs';
          import {spawnSync} from 'node:child_process';
          import {group} from './scripts/test-groups.mjs';
          const excluded=new Set([
            'history-performance-v090.test.mjs',
            'light-coverage.test.mjs',
            'smart-filter-diagnostics.test.mjs'
          ]);
          const files=readdirSync('tests').filter(name=>name.endsWith('.test.mjs')&&group(name)==='unit'&&!excluded.has(name)).sort().map(name=>'tests/'+name);
          console.log('Explicitly excluded non-portable fixtures:',[...excluded].join(', '));
          console.log('Running portable unit files:',files.length);
          const result=spawnSync(process.execPath,['--test','--test-concurrency=4',...files],{stdio:'inherit'});
          process.exit(result.status??1);
          NODE
      - name: Package audit
        run: npm run check
      - name: Release build verification
        run: npm run build:release
