import {setProductState} from './product-state.js';
import {request,element} from './common.js';
import {BACKUP_LIMITS,BackupValidator,backupError} from '../core/backup-format.js';
const $=id=>document.getElementById(id);
export const backupMessage=code=>({BACKUP_BUSY:'请等待当前整理或组织变更结束，再创建备份。',BACKUP_CHANGED:'备份期间内容发生变化，本次未生成文件。请重新创建备份。',BACKUP_INVALID:'这不是有效的 PAIA 备份，未修改任何内容。',BACKUP_VERSION_UNSUPPORTED:'此备份版本暂不支持，未修改任何内容。',BACKUP_INTEGRITY_FAILED:'完整性校验未通过，文件可能已损坏。',BACKUP_INCOMPLETE:'备份文件不完整，请重新选择完整文件。',BACKUP_TOO_LARGE:'本版单次恢复支持最多 64 MB / 100000 项；文件超出范围，原有内容保持不变。',BACKUP_SESSION_EXPIRED:'本次备份会话已中断，请重新开始。',BACKUP_TARGET_NOT_EMPTY:'当前已有用户数据。为保护更新的内容，本版仅支持恢复到空库，不会覆盖现有工作。',BACKUP_PURGE_CONFLICT:'此备份包含本机已永久删除的来源，不能恢复。永久删除标记优先。',BACKUP_CONFIRMATION_REQUIRED:'请先查看预览并明确确认恢复。',STORAGE_FULL:'本机存储空间不足，原有内容保持不变。',STORAGE_FAILED:'本机保存未完成，请重新打开 Settings 检查。'}[code]||'操作未完成，未自动重试。请重新选择备份文件。');
export function downloadParts(parts,name,type){const url=URL.createObjectURL(new Blob(parts,{type})),a=element('a');a.href=url;a.download=name;document.body.append(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),60000);}
export async function* backupFileRows(file){if(file.size>BACKUP_LIMITS.restoreBytes)backupError('BACKUP_TOO_LARGE');const reader=file.stream().getReader(),decoder=new TextDecoder('utf-8',{fatal:true});let buffer='';try{for(;;){const {done,value}=await reader.read();buffer+=decoder.decode(value,{stream:!done});let end;while((end=buffer.indexOf('\n'))>=0){const line=buffer.slice(0,end);buffer=buffer.slice(end+1);if(line.length>BACKUP_LIMITS.lineBytes)backupError('BACKUP_TOO_LARGE');if(line.trim()){try{yield JSON.parse(line);}catch{backupError('BACKUP_INVALID');}}}if(buffer.length>BACKUP_LIMITS.lineBytes)backupError('BACKUP_TOO_LARGE');if(done)break;}if(buffer.trim()){try{yield JSON.parse(buffer);}catch{backupError('BACKUP_INVALID');}}}finally{await reader.cancel().catch(()=>{});reader.releaseLock();}}
export class BackupPanel {
 constructor(){this.busy=false;this.sessionId=null;$('backup-create').addEventListener('click',()=>void this.create());$('backup-choose').addEventListener('click',()=>$('backup-file').click());$('backup-file').addEventListener('change',()=>void this.inspect($('backup-file').files?.[0]));$('backup-restore').addEventListener('click',()=>void this.restore());$('backup-cancel').addEventListener('click',()=>void this.cancel());}
 status(text,state){$('backup-status').textContent=text;this.currentState=state||(text.startsWith('正在')?'loading':text.includes('已完成')||text.includes('已生成')?'saved':text?'ready':'idle');setProductState($('backup-settings'),this.currentState);}
 lock(value){this.busy=value;setProductState($('backup-settings'),value?'loading':this.currentState||'idle');for(const id of ['backup-create','backup-choose','backup-restore'])$(id).disabled=value;}
 async cancel(){if(this.busy)return;if(this.sessionId)await request('PAIA_BACKUP_CANCEL',{options:{sessionId:this.sessionId}}).catch(()=>{});this.sessionId=null;this.preview=null;$('backup-preview').hidden=true;$('backup-file').value='';this.status('');}
 async create(){if(this.busy)return;await this.cancel();this.lock(true);let sessionId;try{this.status('正在分段创建本地备份…');const begin=await request('PAIA_BACKUP_BEGIN_EXPORT');sessionId=begin.sessionId;const parts=[JSON.stringify(begin.header)+'\n'];let sequence=0,count=0;for(;;){const page=await request('PAIA_BACKUP_EXPORT_PAGE',{options:{sessionId,sequence:sequence++}});parts.push(page.items.map(x=>JSON.stringify(x)+'\n').join(''));count+=page.items.filter(x=>x.type==='item').length;this.status(`正在创建备份 · 已处理 ${count} 项`);if(page.done)break;await new Promise(resolve=>setTimeout(resolve,0));}downloadParts(parts,'PAIA-Backup-'+new Date().toISOString().replace(/[:.]/g,'-')+'.paia-backup','application/x-ndjson');const restoreLimit=count>BACKUP_LIMITS.restoreItems||new Blob(parts).size>BACKUP_LIMITS.restoreBytes;this.status(restoreLimit?'备份已生成并开始下载。此文件超出本版单次恢复范围（64 MB / 100000 项），请保留原库；本次没有删除或覆盖内容。':'备份已生成并开始下载。文件包含私人数据，请自行妥善保存。');}catch(e){this.status(backupMessage(e.code),'failed');}finally{if(sessionId)await request('PAIA_BACKUP_CANCEL',{options:{sessionId}}).catch(()=>{});this.lock(false);$('backup-restore').disabled=!this.preview?.canRestore;}}
 async inspect(file){if(!file||this.busy)return;await this.cancel();this.lock(true);try{this.status('正在本机校验备份…');const begin=await request('PAIA_BACKUP_BEGIN_RESTORE');this.sessionId=begin.sessionId;const validator=new BackupValidator();let batch=[];for await(const row of backupFileRows(file)){await validator.add(row);batch.push(row);if(batch.length===30){await request('PAIA_BACKUP_STAGE',{options:{sessionId:this.sessionId,items:batch}});batch=[];this.status(`正在校验 · ${validator.count} 项`);}}validator.preview();if(batch.length)await request('PAIA_BACKUP_STAGE',{options:{sessionId:this.sessionId,items:batch}});this.preview=await request('PAIA_BACKUP_PREVIEW',{options:{sessionId:this.sessionId}});const p=this.preview;$('backup-preview-content').replaceChildren(element('p','',`备份日期：${new Date(p.createdAt).toLocaleString()}`),element('p','',`PAIA ${p.appVersion} · 备份格式 ${p.formatVersion}`),element('p','',`${p.counts.inputs} 条 Input · ${p.counts.topics} 个 Topic · ${p.counts.entries} 条思想内容 · ${p.counts.revisions} 个版本`),element('p','muted',p.canRestore?'完整性校验通过。确认后将在本机恢复这些数据，API Key 与捕获授权保持当前设置。':backupMessage(p.reason)));$('backup-preview').hidden=false;this.status(p.canRestore?'请核对备份信息，然后点击“确认恢复到空库”。':'校验通过，当前库不满足安全恢复条件。');}catch(e){this.status(backupMessage(e.code),'failed');if(this.sessionId)await request('PAIA_BACKUP_CANCEL',{options:{sessionId:this.sessionId}}).catch(()=>{});this.sessionId=null;this.preview=null;}finally{this.lock(false);$('backup-restore').disabled=!this.preview?.canRestore;}}
 async restore(){if(this.busy||!this.preview?.canRestore)return;this.lock(true);try{this.status('正在恢复，请保持本页打开…');await request('PAIA_BACKUP_RESTORE',{options:{sessionId:this.sessionId,confirmation:this.preview.integrity}});this.sessionId=null;this.preview=null;$('backup-preview').hidden=true;this.status('恢复已完成。内容与版本已保存到本机；本地搜索索引正在更新。');}catch(e){this.status(backupMessage(e.code),'failed');}finally{this.lock(false);$('backup-restore').disabled=true;}}
}
