import {request,element} from './common.js';
import {textOf,AutosaveSession,UndoJournal,RevisionSession} from './editor-primitives.js';
const labels={blockSummary:'主题速览',currentView:'当前理解',keyInformation:'核心信息',decisions:'重要决定',preferences:'偏好与原则',judgments:'当前判断',openQuestions:'待解决问题',possibleEvolution:'可能的变化'};
export class AIReadingEditor {
 constructor(root,presentation,onEvidence,onStatus){this.root=root;this.row=structuredClone(presentation);this.onStatus=onStatus;this.pending=null;this.timer=null;this.failed=false;this.disposed=false;this.nodes=new Map();this.controller=new AbortController();this.autosave=new AutosaveSession(()=>void this.flush(),{delay:650,maxWait:3000});this.journal=new UndoJournal();this.revisions=new RevisionSession();this.draft={};const options={signal:this.controller.signal};
  for(const [field,label]of Object.entries(labels)){if(Array.isArray(this.row[field])?!this.row[field].length:!this.row[field]?.trim())continue;const section=element('section','ai-reading-section');section.append(element('h2','',label));const values=Array.isArray(this.row[field])?this.row[field]:[{text:this.row[field],evidenceEntryIds:this.row.evidenceEntryIds}];const nodes=[];
   for(const value of values){const item=element('div','ai-statement'),prose=element('div','entry-prose',value.text);prose.contentEditable='plaintext-only';prose.setAttribute('aria-label',label);prose.dataset.aiField=field;nodes.push(prose);item.append(prose);const details=element('details','ai-evidence');details.append(element('summary','','依据 · '+value.evidenceEntryIds.length+' 条内容'));value.evidenceEntryIds.forEach((id,i)=>{const b=element('button','','查看依据 '+(i+1));b.type='button';b.addEventListener('click',()=>void Promise.resolve(onEvidence(id)).catch(()=>this.onStatus('依据尚未打开，现有内容保留。请再试一次。','error')),options);details.append(b);});item.append(details);section.append(item);prose.addEventListener('compositionstart',()=>{this.composing=true;this.autosave.cancel();},options);prose.addEventListener('compositionend',()=>{this.composing=false;this.schedule();},options);prose.addEventListener('input',()=>{if(!this.composing)this.schedule();},options);prose.addEventListener('blur',()=>{if(!this.composing)void this.flush();},options);}
   this.nodes.set(field,nodes);this.draft[field]=this.values(field);root.append(section);
  }
  root.addEventListener('keydown',e=>{if((e.metaKey||e.ctrlKey)&&!e.altKey&&['z','y'].includes(e.key.toLowerCase())){e.preventDefault();void this.history(e.shiftKey||e.key.toLowerCase()==='y');}},options);
 }
 collect(){if(this.composing||this.disposed)return;const changes=[];for(const field of this.nodes.keys()){const value=this.values(field);if(JSON.stringify(value)!==JSON.stringify(this.draft[field]))changes.push({field,before:this.draft[field],after:value});}if(changes.length){this.journal.record(changes);for(const c of changes)this.draft[c.field]=structuredClone(c.after);}}
 async history(redo=false){if(this.composing)return;this.collect();const from=redo?this.journal.redo:this.journal.undo,to=redo?this.journal.undo:this.journal.redo;if(!from.length)return;const changes=from.pop();to.push(changes);for(const c of changes){const value=structuredClone(redo?c.after:c.before);this.draft[c.field]=value;this.nodes.get(c.field).forEach((node,i)=>node.textContent=Array.isArray(value)?value[i].text:value);}this.failed=false;await this.flush();}
 values(field){const nodes=this.nodes.get(field);return Array.isArray(this.row[field])?nodes.map((n,i)=>({...this.row[field][i],text:textOf(n)})):textOf(nodes[0]);}
 dirty(){return [...this.nodes.keys()].some(k=>JSON.stringify(this.values(k))!==JSON.stringify(this.row[k]));}
 schedule(){if(this.disposed)return;this.collect();this.failed=false;this.onStatus('正在保存…');this.autosave.schedule();}
 async flush(){this.autosave.cancel();if(this.composing||this.disposed||this.conflicted)return false;if(this.pending){await this.pending;return this.dirty()?this.flush():true;}if(this.failed)return false;this.pending=(async()=>{for(const field of this.nodes.keys()){const value=this.values(field);if(JSON.stringify(value)===JSON.stringify(this.row[field]))continue;try{const r=await request('EDIT_AI_PRESENTATION',{edit:this.revisions.attempt({topicId:this.row.topicId,field,value,expectedRevision:this.row.revision})});this.row[field]=value;this.row.revision=r.revision;this.onStatus('已保存 · 此字段由你维护');}catch(e){this.failed=true;this.conflicted=e.code==='STALE_BASE';this.onStatus(this.conflicted?'此字段已有较新的版本，当前草稿仍在页面。请重新读取已保存版本，或保留草稿后再编辑。':'AI整理修改尚未保存，当前草稿仍在页面。请重试本地保存，不会调用 AI。',this.conflicted?'conflict':'error');return false;}}return true;})();let ok;try{ok=await this.pending;}finally{this.pending=null;}return ok&&this.dirty()?this.flush():ok;}
 get saving(){return !!this.pending;}
 dispose(){this.autosave.dispose();this.controller.abort();this.journal.clear();this.disposed=true;for(const nodes of this.nodes.values())for(const node of nodes)node.remove();}
}
