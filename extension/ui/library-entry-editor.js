import {thoughtCopy as tc} from './thought-copy.js';
import {request} from './common.js';
import {PlainTextSurface,AutosaveSession,UndoJournal,RevisionSession,textOf} from './editor-primitives.js';
import {RecoveryDraftSession,clearRecoveryDrafts} from './recovery-draft.js';
const fields=['body','note','type'];
const snapshot=e=>Object.fromEntries(fields.map(f=>[f,e[f]]));
const equal=(a,b)=>JSON.stringify(a)===JSON.stringify(b);
export class LibraryEntryEditor {
 constructor(root,rows,onStatus,onSaved=()=>{}){
  this.root=root;this.onStatus=onStatus;this.onSaved=onSaved;this.entries=new Map(rows.map(e=>[e.id,{saved:snapshot(e),local:snapshot(e),revision:e.revision,fieldRevisions:e.fieldRevisions,currentInputRevision:e.currentInputRevision,sourceRecordIds:[...new Set(e.sourceRecordIds||[])]}]));this.recoveries=new Map(rows.map(e=>[e.id,new RecoveryDraftSession({kind:'library_entry',ownerId:e.id,sourceRecordIds:[...new Set(e.sourceRecordIds||[])]})]));this.recoveryChecked=new Set();this.recoveryOps=new Map();this.recoveryFailed=false;this.journal=new UndoJournal();this.revisions=new RevisionSession();this.autosave=new AutosaveSession(()=>void this.flush());this.controller=new AbortController();const options={signal:this.controller.signal};
  this.surface=new PlainTextSurface(root,{start:event=>{this.composingId=event.target.closest('[data-entry-id]')?.dataset.entryId;this.autosave.cancel();},end:()=>{if(this.compositionCanceled){this.compositionCanceled=false;this.paint(this.composingId);this.composingId=null;const next=this.revisions.take();if(next)this.receive(next);return;}this.collect();const next=this.revisions.take();if(next)this.receive(next);},input:()=>this.collect(),leave:()=>{this.collect();void this.flush();},paste:e=>this.paste(e)});
  root.addEventListener('keydown',e=>{if((e.metaKey||e.ctrlKey)&&!e.altKey&&(e.key.toLowerCase()==='z'||e.key.toLowerCase()==='y')){e.preventDefault();void this.history(e.shiftKey||e.key.toLowerCase()==='y');}},options);
  root.addEventListener('beforeinput',e=>this.beforeInput(e),options);root.addEventListener('drop',e=>{if(e.target.closest('[data-entry-field]'))e.preventDefault();},options);this.onStatus(tc('已保存到本机'));this.recoveryReady=this.restoreRecovery();
 }
 field(id,key){return [...this.root.querySelectorAll('[data-entry-id]')].find(el=>el.dataset.entryId===id)?.querySelector(`[data-entry-field="${key}"]`);}
 dirty(){return [...this.entries.values()].some(e=>!equal(e.saved,e.local));}
 collect(){if(this.surface.composing||this.disposed)return;const patches=[];for(const [id,e]of this.entries)for(const f of fields){const el=this.field(id,f);if(!el)continue;const after=textOf(el);if(e.local[f]!==after)patches.push({id,field:f,before:e.local[f],after});}this.change(patches);}
 change(patches,record=true){if(!patches.length)return;if(record)for(let i=0;i<patches.length;i+=40)this.journal.record(patches.slice(i,i+40));for(const p of patches)this.entries.get(p.id).local[p.field]=p.after;this.failed=false;if(!this.conflicted)this.onStatus(tc('正在保存…'));this.autosave.schedule();this.scheduleRecoveryProtection();}
 scheduleRecoveryProtection(){if(this.recoveryScheduled||this.disposed)return;this.recoveryScheduled=true;queueMicrotask(()=>{this.recoveryScheduled=false;if(!this.disposed)void this.protectRecovery();});}
  paint(onlyId=null){for(const [id,e]of this.entries)for(const f of fields){if(onlyId&&id!==onlyId)continue;const el=this.field(id,f);if(el&&textOf(el)!==e.local[f]){if(el.matches('input,textarea,select'))el.value=e.local[f];else el.textContent=e.local[f];}}}
 async history(redo=false){if(this.surface.composing)return;this.collect();const from=redo?this.journal.redo:this.journal.undo,to=redo?this.journal.undo:this.journal.redo;if(!from.length)return;const patches=from.at(-1);if(patches.savedEdit&&!this.dirty()){
   try{const items=[];for(const ref of patches.savedEdit){const row=await request('GET_LIBRARY_ENTRY',{id:ref.id}),known=this.entries.get(ref.id);if(known&&row.revision!==known.revision){this.onStatus(tc('内容已变化，请核对版本历史。'),'conflict');return;}items.push({...ref,side:redo?'after':'before',expectedRevision:row.revision,expectedInputRevision:row.currentInputRevision});}let edit={operationId:crypto.randomUUID(),items},result=await request('THOUGHT_EDIT_HISTORY',{edit});if(result.requiresArchiveConfirmation){if(!window.confirm(tc('这次撤销也会修改档案。仅确认本次操作，不会开启全局开关。')))return;result=await request('THOUGHT_EDIT_HISTORY',{edit:{...edit,confirmArchive:true}});}if(result.conflict){this.onStatus(tc('档案或思想已变化，撤销未写入。'),'conflict');return;}for(const ref of patches.savedEdit){const row=await request('GET_LIBRARY_ENTRY',{id:ref.id});this.entries.set(ref.id,{saved:snapshot(row),local:snapshot(row),revision:row.revision,fieldRevisions:row.fieldRevisions,currentInputRevision:row.currentInputRevision});}from.pop();to.push(patches);this.paint();this.onStatus(tc('已保存到本机'));this.onSaved();return;}catch{this.onStatus(tc('撤销尚未保存，文字仍在这里。'),'error');return;}
  }for(const id of new Set(patches.map(p=>p.id))){if(!this.entries.has(id)){let row;try{row=await request('GET_LIBRARY_ENTRY',{id});}catch{this.journal.clear();this.onStatus('相关内容已不可用，撤销记录已清理。');return;}const expected=this.historyVersions?.[id];if(row.lifecycle!=='active'||row.staleReasons?.includes('source_purged')||!expected||!equal(expected,row.fieldRevisions)){this.journal.clear();this.onStatus('相关内容已变化，请重新查看版本历史。');return;}this.entries.set(id,{saved:snapshot(row),local:snapshot(row),revision:row.revision,fieldRevisions:row.fieldRevisions,currentInputRevision:row.currentInputRevision});}}from.pop();this.change(patches.map(p=>({...p,after:redo?p.after:p.before})),false);to.push(patches);this.paint();this.reason=redo?'redo':'undo';await this.flush();}
 beforeInput(e){if(e.inputType==='historyUndo'||e.inputType==='historyRedo'){e.preventDefault();void this.history(e.inputType==='historyRedo');return;}if(this.surface.composing||e.isComposing)return;this.crossSelection(e);}
 crossSelection(event,replacement){const selection=getSelection();if(!selection?.rangeCount||selection.isCollapsed)return false;const range=selection.getRangeAt(0);if(!this.root.contains(range.startContainer)||!this.root.contains(range.endContainer))return false;const nodes=[...this.root.querySelectorAll('[data-entry-field="body"]')].filter(el=>range.intersectsNode(el));if(nodes.length<2)return false;event.preventDefault();if(replacement===undefined&&!event.inputType?.startsWith('delete')&&!['insertText','insertReplacementText'].includes(event.inputType))return true;
  const patches=nodes.map((el,i)=>{let prefix='',suffix='';if(el.contains(range.startContainer)){const r=range.cloneRange();r.selectNodeContents(el);r.setEnd(range.startContainer,range.startOffset);prefix=r.toString();}if(el.contains(range.endContainer)){const r=range.cloneRange();r.selectNodeContents(el);r.setStart(range.endContainer,range.endOffset);suffix=r.toString();}const id=el.closest('[data-entry-id]').dataset.entryId;return {id,field:'body',before:this.entries.get(id).local.body,after:prefix+(i===0?(replacement??(event.inputType.startsWith('insert')?event.data||'':'')):'')+suffix};});this.change(patches);this.paint();selection.removeAllRanges();nodes[0].focus();return true;
 }
 paste(e){if(this.surface.composing)return;const target=e.target.closest('[data-entry-field]');if(!target)return;const text=e.clipboardData?.getData('text/plain');if(text===undefined)return;if(this.crossSelection(e,text))return;e.preventDefault();if(target.matches('textarea,input'))target.setRangeText(text,target.selectionStart,target.selectionEnd,'end');else{const selection=getSelection();if(!selection?.rangeCount)return;const range=selection.getRangeAt(0);if(!target.contains(range.startContainer)||!target.contains(range.endContainer))return;range.deleteContents();const node=document.createTextNode(text);range.insertNode(node);range.setStartAfter(node);range.collapse(true);selection.removeAllRanges();selection.addRange(range);}this.collect();
 }
 pendingEntries(){return [...this.entries].filter(([,e])=>!equal(e.saved,e.local)).map(([id,e])=>({id,expectedRevision:e.revision,expectedFieldRevisions:e.fieldRevisions,expectedInputRevision:e.currentInputRevision,changes:Object.fromEntries(fields.filter(f=>e.saved[f]!==e.local[f]).map(f=>[f,e.local[f]]))}));}
 recoveryEdit(entry){const base={entries:[entry],...(this.reason?{reason:this.reason}:{})},signature=JSON.stringify(base),prior=this.recoveryOps.get(entry.id);if(!prior||prior.signature!==signature)this.recoveryOps.set(entry.id,{signature,operationId:crypto.randomUUID()});return {...base,operationId:this.recoveryOps.get(entry.id).operationId};}
 async protectRecovery(entries=this.pendingEntries()){const tokens=new Map(),writes=[];for(const entry of entries){const edit=this.recoveryEdit(entry),session=this.recoveries.get(entry.id);if(!session)continue;tokens.set(entry.id,edit.operationId);writes.push(session.protect({type:'EDIT_LIBRARY_BATCH',edit},edit.operationId).catch(()=>{this.recoveryFailed=true;}));}await Promise.all(writes);return tokens;}
 async restoreRecovery(ids=[...this.recoveries.keys()]){const drafts=[];for(const id of ids){if(this.recoveryChecked.has(id))continue;this.recoveryChecked.add(id);const session=this.recoveries.get(id);if(!session)continue;try{const draft=await session.load();if(draft)drafts.push([id,draft]);}catch{}}for(const [id,draft]of drafts){const e=this.entries.get(id),edit=draft.operation?.edit,entry=edit?.entries?.length===1?edit.entries[0]:null;if(!e||draft.operation?.type!=='EDIT_LIBRARY_BATCH'||entry?.id!==id){await this.recoveries.get(id)?.clear(draft.token).catch(()=>{});continue;}const already=Object.entries(entry.changes||{}).every(([field,value])=>e.saved[field]===value);if(already){await this.recoveries.get(id).clear(draft.token).catch(()=>{});continue;}const baseMatches=Object.keys(entry.changes||{}).every(field=>e.fieldRevisions?.[field]===entry.expectedFieldRevisions?.[field]);if(!baseMatches){Object.assign(e.local,entry.changes);this.paint(id);this.conflicted=true;this.onStatus('检测到上次未完成的思想修改，但已保存版本同时变化。草稿已恢复到页面，未自动覆盖。','conflict');continue;}try{const result=await request('EDIT_LIBRARY_BATCH',{edit});if(result?.conflict){Object.assign(e.local,entry.changes);this.paint(id);this.conflicted=true;this.onStatus('检测到上次未完成的思想修改，但已保存版本同时变化。草稿已恢复到页面，未自动覆盖。','conflict');continue;}const item=result.items?.find(x=>x.id===id);if(item){Object.assign(e.saved,entry.changes);Object.assign(e.local,entry.changes);e.revision=item.revision;e.fieldRevisions=item.fieldRevisions;e.currentInputRevision=item.currentInputRevision;this.paint(id);}await this.recoveries.get(id).clear(draft.token).catch(()=>{});this.onStatus('已恢复上次未完成的思想修改');this.onSaved();}catch(error){if(error?.code==='INVALID_REQUEST'){await this.recoveries.get(id).clear(draft.token).catch(()=>{});continue;}Object.assign(e.local,entry.changes);this.paint(id);this.failed=true;this.onStatus('检测到上次未完成的思想修改。草稿仍保存在本机，可重试。','error');}}return drafts.length>0;}
 get recoveryPending(){return !!this.recoveryScheduled||[...this.recoveries.values()].some(session=>session.pending);}
 async flush(){this.autosave.cancel();if(this.disposed||this.surface.composing||this.failed||this.conflicted)return false;if(this.saving){await this.pending;return this.dirty()?this.flush():!this.conflicted;}if(!this.dirty())return true;
  const pendingEntries=this.pendingEntries();
  this.saving=true;
  this.pending=(async()=>{const recoveryTokens=await this.protectRecovery(pendingEntries);try{
   let detached=false;
   for(let offset=0;offset<pendingEntries.length;offset+=40){
    const entries=pendingEntries.slice(offset,offset+40),edit=this.revisions.attempt({entries,...(this.reason?{reason:this.reason}:{})}),r=await request('EDIT_LIBRARY_BATCH',{edit});
    if(r.conflict){this.conflicted=true;this.onStatus('其他页面已修改相同内容，当前草稿尚未保存。','conflict');return false;}
    const recoveryClears=[];for(const item of r.items){const e=this.entries.get(item.id),sent=entries.find(x=>x.id===item.id);if(!e||!sent)continue;Object.assign(e.saved,sent.changes);e.revision=item.revision;e.fieldRevisions=item.fieldRevisions;e.currentInputRevision=item.currentInputRevision;detached||=!!item.detached;const token=recoveryTokens.get(item.id);if(token)recoveryClears.push({kind:'library_entry',ownerId:item.id,token});}
    if(recoveryClears.length)await clearRecoveryDrafts(recoveryClears).catch(()=>{});
    if(r.items.some(x=>x.revisionId))for(const ref of r.items.filter(x=>x.revisionId).map(x=>({id:x.id,revisionId:x.revisionId}))){for(let i=this.journal.undo.length-1;i>=0;i--){const undoGroup=this.journal.undo[i];if(!undoGroup.some(p=>p.id===ref.id))continue;undoGroup.savedEdit=[...(undoGroup.savedEdit||[]).filter(x=>x.id!==ref.id),ref];break;}}
   }
   this.reason=null;this.onStatus(this.dirty()?tc('正在保存…'):detached?tc('已修改这条思想，档案未变。'):tc('已保存到本机'));return true;
  }catch{this.failed=true;this.onStatus(this.recoveryFailed?'尚未保存；恢复草稿也未能完整写入。请保持此页打开并重试或复制文字。':'尚未保存。已成功写入的批次保留，其余草稿已保存在本机，可重试。','error');return false;}finally{this.saving=false;const incoming=this.revisions.take();if(incoming)this.receive(incoming);}})();
  const ok=await this.pending;if(ok&&this.dirty())return this.flush();if(ok)this.onSaved();return ok;
 }
 protectedIds(){
  this.collect();const ids=new Set();for(const [id,e]of this.entries)if(!equal(e.saved,e.local))ids.add(id);
  if(this.composingId)ids.add(this.composingId);
  const active=globalThis.document?.activeElement?.closest?.('[data-entry-id]');if(active?.dataset.entryId)ids.add(active.dataset.entryId);
  const selection=globalThis.getSelection?.();if(selection?.rangeCount&&!selection.isCollapsed){const range=selection.getRangeAt(0);for(const node of this.root.querySelectorAll('[data-entry-id]')){try{if(range.intersectsNode(node))ids.add(node.dataset.entryId);}catch{}}}
  for(const node of this.root.querySelectorAll('[data-entry-id]'))if(node.dataset.mobileEditing==='true'||node.querySelector('details[open],.library-actions[open]'))ids.add(node.dataset.entryId);
  return ids;
 }
 releaseRows(keep){
  const protectedIds=this.protectedIds(),kept=new Set([...(keep||[]),...protectedIds]),released=[];
  this.historyVersions??={};
  for(const [id,e]of [...this.entries]){if(kept.has(id))continue;this.historyVersions[id]=e.fieldRevisions;this.entries.delete(id);released.push(id);}
  return {released,protectedIds};
 }
 async checkTracked(rows){for(const r of rows){const e=this.entries.get(r.id);if(!e)continue;if(r.lifecycle!=='active'){if(this.composingId===r.id){this.surface.composing=false;this.composingId=null;this.compositionCanceled=false;}this.journal.clear();this.field(r.id,'body')?.closest('[data-entry-id]')?.remove();this.entries.delete(r.id);continue;}if(r.purged&&!e.purgeCleared){if(this.surface.composing&&this.composingId===r.id){this.compositionCanceled=true;this.autosave.cancel();}const safe=await request('GET_LIBRARY_ENTRY',{id:r.id});this.journal.clear();e.saved=snapshot(safe);e.local=snapshot(safe);e.revision=safe.revision;e.fieldRevisions=safe.fieldRevisions;e.purgeCleared=true;this.paint(r.id);const provenance=this.field(r.id,'body')?.closest('[data-entry-id]')?.querySelector('.entry-provenance');if(provenance){provenance.open=false;provenance.lastElementChild.replaceChildren();}}}}
 addRows(rows){const added=[];for(const row of rows)if(!this.entries.has(row.id)){this.entries.set(row.id,{saved:snapshot(row),local:snapshot(row),revision:row.revision,fieldRevisions:row.fieldRevisions,currentInputRevision:row.currentInputRevision,sourceRecordIds:[...new Set(row.sourceRecordIds||[])]});this.recoveries.set(row.id,new RecoveryDraftSession({kind:'library_entry',ownerId:row.id,sourceRecordIds:[...new Set(row.sourceRecordIds||[])]}));added.push(row.id);}if(added.length)void this.restoreRecovery(added);}
 receive(rows){if(this.saving||this.surface.composing){this.revisions.defer(rows);return;}for(const row of rows){const e=this.entries.get(row.id);if(!e)continue;if(row.lifecycle!=='active'||row.staleReasons?.includes('source_purged')){this.journal.clear();if(row.lifecycle!=='active'){this.entries.delete(row.id);this.field(row.id,'body')?.closest('[data-entry-id]').remove();continue;}}
   if(row.revision<=e.revision){if(!this.dirty())e.currentInputRevision=row.currentInputRevision;continue;}const external=fields.some(f=>row.fieldRevisions[f]!==e.fieldRevisions[f]);if(external){this.journal.undo=this.journal.undo.filter(group=>!group.some(p=>p.id===row.id));this.journal.redo=this.journal.redo.filter(group=>!group.some(p=>p.id===row.id));}const conflict=RevisionSession.conflicts(e.saved,e.local,e.fieldRevisions,row,fields);if(conflict.length){this.conflicted=true;this.onStatus('其他页面已修改相同内容，当前草稿尚未保存。','conflict');continue;}for(const f of fields){if(e.local[f]===e.saved[f]){e.local[f]=row[f];const el=this.field(row.id,f);if(el&&textOf(el)!==row[f]){if(el.matches('input,textarea,select'))el.value=row[f];else el.textContent=row[f];}}e.saved[f]=row[f];}e.revision=row.revision;e.fieldRevisions=row.fieldRevisions;e.currentInputRevision=row.currentInputRevision;
  }}
 rebaseConflict(rows){
  if(!this.conflicted||this.surface.composing||!Array.isArray(rows)||!rows.length)return false;
  const byId=new Map();
  for(const row of rows){
   const current=this.entries.get(row?.id);
   const oldSources=[...(current?.sourceRecordIds||[])].sort(),newSources=[...(row?.sourceRecordIds||[])].sort();
   if(!current||row.lifecycle!=='active'||row.staleReasons?.includes('source_purged')||!row.fieldRevisions||!Number.isInteger(row.revision)||!(row.currentInputRevision===null||Number.isInteger(row.currentInputRevision))||JSON.stringify(oldSources)!==JSON.stringify(newSources)||!fields.some(field=>current.local[field]!==current.saved[field]))return false;
   byId.set(row.id,row);
  }
  if(byId.size!==rows.length)return false;
  for(const [id,row] of byId){
   const current=this.entries.get(id);
   for(const field of fields){
    if(current.local[field]===current.saved[field])current.local[field]=row[field];
    current.saved[field]=row[field];
   }
   current.revision=row.revision;current.fieldRevisions=row.fieldRevisions;current.currentInputRevision=row.currentInputRevision;
   this.paint(id);
  }
  this.journal.clear();this.revisions.last=null;this.recoveryOps.clear();
  this.reason=null;this.failed=false;this.conflicted=false;
  return true;
 }
 exportHistory(){const undo=structuredClone(this.journal.undo),redo=structuredClone(this.journal.redo),versions={...this.historyVersions};for(const [id,e]of this.entries)versions[id]=e.fieldRevisions;const ids=new Set([...undo,...redo].flatMap(g=>g.map(p=>p.id)));return {undo,redo,versions:Object.fromEntries([...ids].map(id=>[id,versions[id]]))};}
 importHistory(h){if(!h)return;this.journal.undo=h.undo;this.journal.redo=h.redo;this.historyVersions=h.versions;for(const [id,e]of this.entries)if(h.versions[id]&&!equal(h.versions[id],e.fieldRevisions)){this.journal.undo=this.journal.undo.filter(g=>!g.some(p=>p.id===id));this.journal.redo=this.journal.redo.filter(g=>!g.some(p=>p.id===id));}}
 dispose(){this.disposed=true;this.autosave.dispose();this.surface.dispose();this.controller.abort();this.journal.clear();this.entries.clear();}
}