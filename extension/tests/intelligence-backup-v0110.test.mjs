import test from 'node:test';import assert from 'node:assert/strict';import {validateBackupHeader,BACKUP_VERSION,BACKUP_SCHEMA,BACKUP_SECTIONS} from '../core/backup-format.js';
test('v0.11 backup envelope accepts current and prior versions without accepting unknown future versions',()=>{const row={type:'header',format:'PAIA Backup',formatVersion:BACKUP_VERSION,schemaVersion:BACKUP_SCHEMA,appVersion:'0.11.0',createdAt:'2026-09-01T00:00:00Z',contentSections:Object.keys(BACKUP_SECTIONS)};assert.equal(validateBackupHeader(row),row);assert.doesNotThrow(()=>validateBackupHeader({...row,appVersion:'0.10.1'}));assert.throws(()=>validateBackupHeader({...row,appVersion:'0.12.0'}));});
