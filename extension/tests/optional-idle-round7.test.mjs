import test from 'node:test';import assert from 'node:assert/strict';import {readOptionalLibraryStatus} from '../ui/optional-library-status.js';
test('Round 7: a confirmed null bounded job is idle, not an unavailable permission check',async()=>{const r=await readOptionalLibraryStatus(async type=>type==='GET_BOUNDED_ORGANIZER'?null:{state:'ready'});assert.deepEqual(r.unavailable,[]);assert.equal(r.values.bounded,null);assert.deepEqual(r.values.controls,{state:'ready'});});
test('Round 7: idle exception never enables missing controls or failed bounded reads',async()=>{for(const invalid of [undefined,[],false,0]){const r=await readOptionalLibraryStatus(async type=>type==='GET_BOUNDED_ORGANIZER'?invalid:type==='GET_ORGANIZER_CONTROLS'?null:{});assert.ok(r.unavailable.includes('bounded'));assert.ok(r.unavailable.includes('controls'));assert.ok(!Object.hasOwn(r.values,'bounded'));}const failed=await readOptionalLibraryStatus(async type=>{if(type==='GET_BOUNDED_ORGANIZER')throw Error('unavailable');return {};});assert.deepEqual(failed.unavailable,['bounded']);});
