# Round 7 — Thought evolution and ordered recomposition

Development candidate on `round7/evolution-recomposition`, stacked on Round 6 (`186b473b`). Not deployed to the daily profile. No real archive or paid-provider validation is claimed.

## Implemented

Solid neutral reading surfaces across Archive, Thought Library, AI Context and Settings; no scenic art. AI reading prioritizes a saved overview plus long-form evolution. Existing possibleEvolution items provide editable framing with complete cited Thought bodies inline. One editable occurrence per entry, existing CAS/undo/versions and source-purge protections. Dates resolve from trusted source metadata, never capture or generation time. Anchor buttons and a draggable range navigate the continuous document. Up to 80 cited entries per view, eight concurrent reads per batch, per-read timeout. Coverage and missing content are explained. No forced growth story when the cache lacks supported progression.

Older independently authored AI fields remain in a low-frequency disclosure, not deleted or disguised as evolution. Direct editing is retained; user-initiated copy is added without clipboard reads or new permissions. Ordinary reading no longer advertises Source buttons; Settings > Data retains raw data/export/purge. Smart Filter behavior and restore protection remain unchanged.

Cached view changes use a bounded ~720ms ordered frosted transition with final settling. Reduced motion and unsupported browsers use direct updates. User input cancels decoration. No extra provider request, artificial model wait, automatic retry or new network capability.

The existing provider schema remains unchanged. Its prompt emphasizes overview and warranted evolution with actual evidence, preserving conditions and doubt, without inventing dates/stages/replacement. Opening or switching views remains zero-provider.

## Not completed by this round

This is NOT the entire future model migration. Source, Input working text and independently edited Thought text remain separate persistent layers. Inline Thought edits update Thought, not Input. Synthesized prose does not reverse-write historical inputs. AI Context still uses existing Topic authorization/retrieval; direct access to unclassified Input requires a separate reviewed change. Stronger paraphrase settings, native apps, cross-device sync, website rework and actual local installation are not included.

The inherited existing-database gate must be cleared before merging stacked Round 2–6 changes or this candidate into daily use. Confirm a PAIA backup and the original unpacked extension path. Never uninstall or erase the current extension to install this preview.

## Verification

`node scripts/verify-round7.mjs` records exact tested SHA and results under `work/round7`. It runs portable units, adapter/privacy contracts, selected real-Chromium synthetic journeys, source package audit and emitted-release audit. The same three historical/environment-bound unit fixtures documented by previous rounds remain explicit exclusions. Full-suite or real-user acceptance is not implied. Generated screenshots use actual extension code with synthetic data, not image-generation mocks.
