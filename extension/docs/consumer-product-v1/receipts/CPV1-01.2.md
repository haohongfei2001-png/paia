# CPV1-01.2 — Extension/page version handshake and reconnect

status: COMPLETE / PASS

baseline_main: `86c27f7985d33d7045ca7dc57ddac0c03049a8b9`

candidate_pr: `#51`

candidate_head: `7e48eee12565c115875d4724fcabab098b4b134a`

candidate_certification: `PAIA Certification runs 35847312073 and 35850707309 / SUCCESS`

merged_runtime_main: `2fbcf5356d3b023cc476bd5640b260662de26fcd`

exact_main_certification: `PAIA Certification run 35853925872 / SUCCESS`

## User-visible outcome

An old page after an extension update stops capture and presents one clear refresh action instead of silently claiming that capture is healthy. A new page reconnects to the updated extension and keeps the existing archive. A discarded and restored page can capture one new Input without duplicating it.

## Evidence and boundary

- **CODE:** PR #51 added a trusted worker/content-script runtime-version handshake. A stale content script's CAPTURE request is rejected before writing. The page shows a bounded refresh action on extension invalidation; transient reconnect clears the notice. Safe reinjection is used only where duplicate-instance risk is controlled.
- **CI:** Candidate Certification passed at the exact PR head; integrated runtime main Certification run `35853925872` passed on the exact merge SHA above, including the full suite, current browser suite, four unit shards, adapter/privacy contracts, macOS secure-store check and build/guards. Pages build/deployment run `35853925573` also succeeded; publication is not claimed or required for this round.
- **SYNTHETIC_BROWSER:** Dedicated headless Chrome cases covered an old tab after unpacked extension update, a new tab following the update, and discard/restore with exactly one new capture. The exact-main current browser suite passed. This is controlled browser evidence, not CURRENT_LIVE provider-site or physical-device certification.
- **RELIABILITY / DATA INTEGRITY:** The stale version is denied before storage mutation; reconnect and restored-tab journeys preserve the prior archive and avoid duplicate logical Input. Existing source identity, privacy, secure-store and migration guards remained part of exact-main certification.
- **UX:** The default old-page failure gives a consumer action rather than internal runtime jargon. Ordinary connected capture still proceeds through the production extension/worker path.

## Remaining limitations and authorization

Current supported real-site lifecycle evidence belongs to CPV1-01.6 / VS-01 certification and remains pending; prior `chatgpt.com` headless reachability failed with `ERR_CONNECTION_CLOSED`/timeout and is not labeled PASS. Consumer update/distribution UX, broader degraded states, backup protection and full real lifecycle remain assigned to CPV1-01.3 through CPV1-01.6. No new privacy permission, paid service or release publication was authorized or added. Under `TEMPORARY_NIGHT_WHOLE_EXECUTION / MAX_9_ROUNDS`, four rounds are now complete and five remain. The next eligible round is CPV1-01.3 within VS-01.

This documentation-only receipt certifies the runtime merge SHA above; it does not change that runtime.
