# CPV1-01.3 — Consumer update flow: execution baseline

status: ACTIVE / NOT CERTIFIED

start_main: `15ad108eb0a9f63f6c05c076f992f057bcc86d2c`

authorization: `TEMPORARY_NIGHT_WHOLE_EXECUTION / MAX_9_ROUNDS`; four rounds complete, five remain. This document starts the existing VS-01 round and does not consume a completion count.

## Current path verified at start

- `extension/development/Update PAIA.command` requires a GitHub Desktop-synchronized local `main`, runs a local release build, copies it over an existing unpacked Chrome runtime, then asks the user to visit `chrome://extensions` and press Reload. It is a development fallback, not the canonical consumer outcome.
- `extension/scripts/build_current_release.py` builds and audits a package from source. The package checks protect the fixed permission/host boundary. These gates are reused rather than replaced with a weaker packager.
- The current extension manifest has no production distribution identity or update channel. The macOS native-host release documentation likewise requires a final stable extension ID before signed public packaging.
- Chrome's documented ordinary consumer extension distribution uses the Chrome Web Store; self-hosted installation on macOS/Windows requires managed enterprise policy. An unpacked extension is a development installation. Chrome's normal Web Store update lifecycle is automatic and `runtime.onUpdateAvailable` can defer/reload a waiting update; repeated `requestUpdateCheck` polling is discouraged. Sources: [distribution](https://developer.chrome.com/docs/extensions/how-to/distribute), [update lifecycle](https://developer.chrome.com/docs/extensions/develop/concepts/extensions-update-lifecycle), [runtime API](https://developer.chrome.com/docs/extensions/reference/api/runtime).

## Engineering boundary for this round

Build and test the current supported update experience without publishing a release: package/identity and schema preflight, dirty-edit protection, update/reconnect guidance, post-update health, and a data-safe rollback path where compatible. Existing development updater remains fallback. No new network origin, archive upload, extension permission, paid account or publication is authorized by this start record.

Actual consumer distribution and any new Chrome Web Store account or public release remain separate owner-boundary actions. Engineering tests may use unpacked/headless installations, but they cannot be labeled a published consumer update. The round exit remains open until its own canonical conditions and exact-main checks pass; any external-certification deferral must be recorded under the current EXECUTION_PROTOCOL and never labeled PASS.
