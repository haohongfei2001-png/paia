# CI focus and restored Topic maintenance

Execution: `PAIA-CI-FOCUS-20260921-aem01`.
Start main: `e9847d464d7ed2458b89a9ace582b3b03f91b818`.
Branch: `manager/paia-ci-focus-20260921`.
Scope: existing UX-R5 synchronization, ANS05 browser lifecycle cleanup, and
synthetic CI diagnostics/evidence.
This is a separately authorized maintenance execution, not a resumed ANS09, UX-R5
round, or ANS10. The completed ANS package and its historical receipts are unchanged.
No product runtime, permission, schema, provider or authorization behavior changes.

## Observed failure

The docs-only ANS09 closure commit e9847d4 has the same runtime/test content as
certified main4e7d991. Its additional automatic Certification run35566879421 failed:
- Current Browser job106230383397: first UX-R5 recovery case expected focus on
  `ai-presentation-toggle`, but observed an empty active-element ID.
- Full Suite job106230383445: the same case timed out clicking the Thoughts nav.
  The log shows a ready-to-click nav intercepted by the loading main surface and
  then hidden. The viewport was640px after the page-scale check/reload.

These are real failed runs and remain failures. The original ANS09 exact-main
Certification #434/run35564803189 remains the historical evidence for that commit.
This maintenance does not rewrite it as certification of later commits.

## Diagnosis and minimal correction

A real headless-browser MutationObserver captured the Original pane becoming
visible with `{focused:true, disabled:true, checked:false}` on the AI toggle.
Disabling the control can retain its old focus briefly before native blur; the
normal switch finalizer enables and refocuses it after asynchronous reading.
The old focus-only predicate therefore could succeed before switch completion,
then its following assertion could land in the normal blur interval. Recorded
focus events showed disable at3014.2ms, blur3020.4ms and final enable/focus3037.5ms
in one diagnostic run. These timings explain a race, not a required delay.

The test now waits atomically for Original selected, toggle enabled and focus on
that same toggle. The original exact focus assertion and subsequent keyboard
Space back to cached AI content remain unchanged. No delay/retry/timeout was added
to the test and no product focus behavior was changed.

On narrow screens the current Reader intentionally hides primary navigation.
After reload, the old helper could read pre-restoration aria-current and attempt
another navigation while the original route restoration made that target hidden.
The test now waits for the original Topic heading and verifies its actual history
Topic identity after reload. It does not navigate again or force-click hidden UI.
Initial user navigation and all candidate, IME, stale-CAS, provider-failure,
worker-interruption and no-hidden-request assertions remain.

## Validation and limits

- Unmodified test:3/3 locally passed, confirming the failures were intermittent.
- CPU-throttled and delayed-status-response probes passed; the intermediate
  disabled-but-focused state above was captured directly in the real browser.
- A250ms startup-read delay did not reproduce the navigation timeout locally.
  Its diagnosis relies on the retained CI click trace and current mobile CSS;
  no local RED is invented for that failure.
- Corrected full UX-R5 certification file:3/3 PASS, zero fail/skip,8.79seconds.
- All profiles isolated/headless, synthetic archive/provider responses only.
- Exact candidate/main required CI must be read after publication. Focused local
  success is not substituted for those required checks.

Writer holds only this maintenance branch. The manager reviews and publishes main;
completion/publication evidence is recorded separately after required checks.

## Candidate #436: distinct ANS05 timeout and cleanup repair

Candidate `a2ee9569fb828f358e2389677ae8a44b3845540f` ran Certification
[35569879078](https://github.com/haohongfei2001-png/paia/actions/runs/35569879078).
Full Suite job106239064489 passed1207/1207 (unit988, browser63, adapter102,
privacy54), zero fail/skip, with auditPassed and fullSuite true, and digest
`ade0c016ef6b7b7fc97f28005970daed1e5753e81bd10d1dbdeafa096dcfef8b`.
All three UX-R5 cases passed there. Unit, privacy, build and macOS gates passed.

Current Browser job106239064422 instead timed out in ANS05 at06:55:57UTC
on2026-09-21 (the original300000ms case limit), then emitted no further test
result until its40minute job cancellation at07:26:15UTC. UX-R5 was not reached.
This candidate therefore did not pass all required checks; its Full Suite success
is retained as that exact run's evidence, not used to replace Current Browser.

ANS05 locally completed unchanged in13.996seconds. The original operation that
stalled on that runner is not established: no per-stage diagnostic existed, and
this repair does not claim that the underlying intermittent stall was reproduced
or eliminated. A separate cleanup defect is demonstrated: a pending browser
Promise is not cancelled by Node's test timeout, so the body's finally can remain
unreachable and the browser keeps the test-file child alive, blocking the next
file in the serial suite.

A real isolated headless-browser fault injection used a never-resolving
page.evaluate and100ms Node case timeout. With body-finally cleanup only, the
browser required the probe's2500ms rescue close and the process took3.66seconds.
With independently registered test-context after cleanup, the process exited in
0.90seconds without rescue; the next file executed, and the original timeout
remained a failure with exit1. This follows Node's documented
[test lifecycle hook](https://nodejs.org/api/test.html#contextafterfn-options).

ANS05 now registers cleanup with its test lifecycle in addition to the existing
finally. Its300000ms limit and all assertions are unchanged. A tiny JSON records
only fixed test phase and abort status; the Current Browser job uploads it even
on failure. It contains no archive content, browser credentials or user data.
The shared harness, test runner, runtime and gate definitions are unchanged.

The actual patched ANS05 source was also copied to a scratch probe with an
injected unresolved evaluate and a2000ms probe-only limit. It exited1 in2.27s,
reported the original timeout, ran the following file, and wrote
`{"phase":"consent and capture","aborted":true}` without a rescue timer.
That intentionally failed probe is cleanup evidence, not product certification.
The normal patched ANS05 plus complete UX-R5 file passed4/4, zero fail/skip in
13.95seconds; normal ANS05 diagnostics were complete/aborted=false. Package
checks passed9146 guardrails across216 runtime resources. A new candidate must
still pass all required CI before manager publication.
